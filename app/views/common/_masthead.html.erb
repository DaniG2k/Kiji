<div id="masthead">
	<div>
		<p><%= content_tag :strong, Time.zone.now.strftime("%A") %> <%= Time.zone.now.strftime("%B %e, %Y") %></p>
		<%= image_tag("../images/Kiji_logo.png", :width => 100) %>
	</div>
	<h1><%= t 'title' %> <small>Read the news. <%= render 'about' %></small></h1>
</div>